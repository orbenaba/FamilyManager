package Controllers;

import Views.RegisterChildView;

public class MyProfileChildController extends RegisterChildController {
    public MyProfileChildController(RegisterChildView rview) {
        super(rview);
    }
}
